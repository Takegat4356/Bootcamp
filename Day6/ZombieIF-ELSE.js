
  //If Else
  
  var status = 1; //assigns the variable to it
    if (status === 1) {
        console.log = "Alive";
    } else if (status === 2) {
        console.log = "Zombie";
    } else if (status === 3) {
        console.log = "Dead";
    } else if (status === 4) {
        console.log = "Unknown";
    }
